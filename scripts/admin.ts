// Admin scripts - run locally only, not part of the web build
// Usage: npx ts-node scripts/admin.ts create-round 1

export {};
```

Save with **Ctrl+S** then:
```
git add scripts\admin.ts
git commit -m "Fix admin script build error"
git push