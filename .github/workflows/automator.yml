name: EmojiLotto Round Automator

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  manage-rounds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Run automator
        run: echo "EmojiLotto automator running"
